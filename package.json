{
  "name": "tranbot",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpm --filter electron dev",
    "build": "pnpm --filter electron build",
    "sidecar": "pnpm --filter sidecar start",
    "lint": "pnpm -r lint",
    "fmt": "pnpm -r fmt"
  },
  "devDependencies": {
    "electron": "^40.6.1"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["electron", "esbuild", "onnxruntime-node", "sharp"]
  }
}
